enableFeaturePreview("IMPROVED_POM_SUPPORT")

rootProject.name = 'configuration-server'

include 'core', 'utils', 'api', 'server',
        'plugins:file-system-loader',
        'plugins:javascript-expression-formatter',
        'plugins:properties-parser',
        'plugins:json-parser'